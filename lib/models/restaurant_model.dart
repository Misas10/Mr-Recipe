class RestaurantModel {
  String name, img, morada, prodName, prodImg;
  double prodPrice;
  // int quantidade;
  // Map foodList = {};

  RestaurantModel(this.name, this.img, this.morada, this.prodName, this.prodImg,
      this.prodPrice);
}
